normalize :  #must be a unique ID
  path: preprocessing/normalize.R
  short_name: norm
  # input:  [RNA,MET]
  output: [RNA,MET]

ppID :  #must be a unique ID
  path: preprocessing/ID.R
  short_name: ID
  # input:  ANY
  output: ANY

# scScale :  #must be a unique ID
#   path: pre-processing/sc_scale.R
#   short_name: ID
#   # input:  ANY
#   output: [scRNA]
